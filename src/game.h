#pragma once
#include <iostream>

class Game {
private:
	unsigned int state; // State if in menu, in game, game paused etc.
    // rendering engine
    // audio engine
    // controller engine
public:
    Game(void);
    ~Game(void);

};

#include <iostream>

class game {
private:
	unsigned int state; // State if in menu, in game, game paused etc.
    // rendering engine
    // audio engine
    // controller engine
public:
	game();
	~game();
}
